/// Класс, репрезентирующий отдельный блок, которая отображается на главном экране.
class ActionBlock {
  /// Название блока.
  final String title;

  /// Описание блока.
  final String description;

  ActionBlock({
    required this.title,
    required this.description,
  });
}
